{
  "compilerOptions": {
    "module": "CommonJS", // node.js를 평범하게 사용하고 다양한 걸 import, export할 수 있게
    "target": "ES2015", // 어떤 버전의 JavaScript로 컴파일 되고 싶은지
    "sourceMap": true, // sourcemap 처리를 하고 싶은지
    "outDir": "dist"
  },
  // 어떤 파일들이 컴파일 과정에 포함 되는지
  "include": ["src/**/*"],
  // 어떤 파일들이 컴파일 과정에 포함 되지 않는지
  "exclude": ["node_modules"]
}
